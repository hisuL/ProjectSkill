interface:
  display_name: "Technical Design"
  short_description: "Draft staged module-level technical design docs"
  default_prompt: "Use $technical-design to turn the PRD and architecture doc into staged, reviewable technical design docs for a target module."

policy:
  allow_implicit_invocation: true
